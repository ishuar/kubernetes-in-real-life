apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-secrets-operator
spec:
  chart:
    spec:
      chart: external-secrets
      interval: 1m
      sourceRef:
        kind: HelmRepository
        name: external-secrets-operator
        namespace: flux
      version: 0.9.7
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  interval: 5m
  # https://artifacthub.io/packages/helm/external-secrets-operator/external-secrets?modal=values
  values:
    serviceAccount:
      name: sa-external-secrets-operator
      create: true
      annotations:
        azure.workload.identity/client-id: 2c32b29e-cca6-448a-9db5-5ce3ba5456a7
        azure.workload.identity/tenant-id: 62143b67-6780-4dd3-8294-f784d7bac864
    podLabels:
      azure.workload.identity/use: "true"
